# STEEL CRADLE ―鋼の揺り籠― 開発ガイド

このリポジトリは、スペースコロニー争奪戦争を題材にしたコックピット没入型・宇宙戦闘機ゲームの開発プロジェクトです。

## 最重要ルール

1. **原作小説は `docs/steel_cradle_novel_v2.md` が正です。**
   改稿前の v1 はリポジトリから削除済みです。もしどこかで
   `steel_cradle_novel.md`(v2 が付かないほう)を見つけても、**読んではいけません**。
   v1では第二章が「七分間」でGH0STのログ・接続痕が本文に書かれていましたが、
   v2ではそれらを全削除し、章題も「暗闇の回廊」へ変わっています。
   また v2 にしか無い設定があります(戦争の不文律「畑と空気は、撃たない」など)。
   ※ 実際にv1を読んで設計を誤った事故があります。ファイル名の `_v2` を必ず確かめてください。

2. **`docs/steel_cradle_setting.md` が唯一の仕様書です。** 実装判断に迷ったら必ずこの資料を参照してください。特に:
   - 9.3 中核パラメーター(7つ)…ゲームの心臓部
   - 9.6 コックピットUI・操作系…キーマップとレイアウト
   - 11章 開発計画…技術スタックとフェーズ定義
3. **フェーズを飛ばさない。** 現在のフェーズは README.md の「現在地」を参照。フェーズ0では Three.js を使わない(HTML+素のJSのみ)。
4. **開発者はプログラミング未経験です。** コードには日本語コメントを丁寧に付け、専門用語は都度短く説明してください。1回の変更は小さく、動作確認してから次へ。
5. **グラフィックの作り方は、パートによって違う。**
   - **3D戦闘パート**:画像ファイルを作らない。ローポリ実3D(単色フラットシェーディング+輪郭線、初代スターフォックス調)を**コードで生成**する。
   - **UI計器**:HTML/CSS で描く。画像は使わない。
   - **ADVパート(2Dサイドビュー)**:**ここだけは画像を使う。** 640×360・48色のドット絵背景と 48×48 のキャラコマ。規約は `assets/adv/ASSETS.md` が正典。素材は `development_aids/pixel_kobo`(ピクセル工房)で作る。
6. **音の作り方も、種類によって違う。置き場所は2つに分ける。**
   - **効果音(SE)・ADVパートのSE**:**音声ファイルを使わない。** Web Audio API で `src/audio.js` がその場で波形を合成する(`playTone` / `playSweep` / `playNoise` の3部品)。置き場所は `assets/audio/`(将来、合成では作れない音が出てきたとき用。現在は空)。
   - **BGM**:**ここは音声ファイルを使う。** 数十秒の楽曲は合成では作れないため。置き場所は `assets/bgm/`。**戦闘中を含むゲーム全体のBGMがここに入る**(ADV専用ではない)。素材は `development_aids/oto_kobo`(音工房)で ACE-Step 1.5 を回して作る。
   - ループするBGMは「1周ぶんちょうど」(小節の整数倍)に切り出してあるので、再生側は `loop = true` にするだけでよい。**フェードを掛けてはいけない** ― 繰り返すたび音量が凹む。
   - 曲ごとに同名の `.json` を添える(ループの可否・BPM・小節数・継ぎ目の粗さの記録)。ゲームは読まない。作り直すときの手掛かり。

## 技術スタック

- JavaScript(ブラウザ動作、ビルドツールなしで開始)
- フェーズ1以降:Three.js(CDN読み込み)
- デプロイ:Cloudflare Pages(mainブランチへのpushで自動デプロイ、ビルドコマンドなし、出力ディレクトリ = リポジトリルート)
- `index.html` をルートに置くこと(Cloudflare Pagesがそのまま配信する)

## ディレクトリ構成

```
/
├── index.html        … エントリーポイント(Cloudflare Pagesが配信)
├── src/              … JSファイル
├── assets/
│   ├── adv/          … ADVパートのドット絵(稼働中)。規約は assets/adv/ASSETS.md
│   ├── audio/        … 効果音の音声ファイル用(現在は空。SEは audio.js で合成している)
│   └── bgm/          … BGM(稼働中)。ゲーム全体・戦闘中も含む。音工房で作る
├── docs/
│   ├── art/          … パレット原器と制作途中の見本(ゲームは読み込まない)
│   └── *.md          … 仕様書・小説(ゲーム本体には含まれない)
└── CLAUDE.md         … このファイル
```

素材を作る道具はリポジトリの外にある:
`../development_aids/pixel_kobo`(ピクセル工房 ― 絵)
`../development_aids/oto_kobo`(音工房 ― BGM)。
生成 → 縮小 → 48色へ正規化 → 規約チェック まで通しでやる。
ゲーム本体に Python 依存を持ち込まないため、意図的に別リポジトリにしてある。

## 用語ミニ辞典(仕様書頻出語)

- **神器(レガリア)**: 機体に搭載する12基の特殊コア。実在論文に基づく技術設定
- **紋章(エムブレム)**: 中枢AIピュティアの管理権限キー(物語アイテム)
- **7パラメーター**: 電力/熱/推進剤/シールド/HULL/弾数/センサー
- **託宣戦・旗艦戦**: マルチ対戦の2モード(将来実装、今は設計のみ)
